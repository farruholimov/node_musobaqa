import { IDefaultQuery } from "../interface/query.interface";

export const defaultQueryValues: IDefaultQuery = {
    limit: 10,
    offset: 0,
    page: 0,
    order: "DESC",
    orderBy: "created_at"
}
 

 